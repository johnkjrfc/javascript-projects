// Define your Book class here:

class book {
    constructor (title, author, copyrightDate, isbn, numPages, timesChecked, discardStatus) {
        this.title = title;
        this.author = author;
        this.copyrightDate = copyrightDate
        this.isbn = isbn;
        this.numPages = numPages;
        this.timesChecked = timesChecked;
        this.discardStatus = discardStatus
    }
    checkout
}


// Define your Manual and Novel classes here:
class manual extends book {
    constructor(title, author, copyrightDate, isbn, numPages, timesChecked, discardStatus) {
        super(title, author, copyrightDate, isbn, numPages, timesChecked, discardStatus);
    }
    dispose(currentYear){
        if(currentY)
    }
}
class novel extends book{
    constructor(title, author, copyrightDate, isbn, numPages, timesChecked, discardStatus) {
        super(title, author, copyrightDate, isbn, numPages, timesChecked, discardStatus);
    }
}
// Declare the objects for exercises 2 and 3 here:


// Code exercises 4 & 5 here: